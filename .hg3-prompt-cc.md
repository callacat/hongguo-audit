【TASK】HG-3a 审计轮（引擎：Claude Code，你的标识=cc）：把红果壳包审计 CI 工作线从 v7.3.6.32 参数化升级到新底包 v7.3.7.32，跑 CI 全量 diff 并产出机械事实报告。这是红果短剧破解壳净化项目第三轮，工作目录=/home/agent/workspace/hongguo-audit-hg3/cc（gh 仓 callacat/hongguo-audit 的 hg3-cc 分支，已 checkout）。

【背景（已知事实，勿重做）】
- 新外层壳包：Release `samples` 资产 `hongguo-v7.3.7.32-crack.apk`（238,198,144 B，sha256=337ff9417e672eb407ffd42481bd58eb33bea43ddd5762e1217f07597254b16e），24 个 dex，作者同 keystore（外层证书 eae34eaf…，与前两轮一致）
- 新内嵌官方包：Release `samples` 资产 `hongguo-73732-inner-official.apk`（134,334,100 B，sha256=a49cf486fb6c96d7b9169b3fa6a4a020295a8e46b78949b6b30fadc29b955bf9，CN=novel v1+v2 完整=官方原包）
- HG-1/HG-2（旧版本线）已收官：C 类后门=com/b/a 设备指纹链；画像基线 docs/profile-16a-truly-added.txt（番茄壳 1249 类）；现有 workflow .github/workflows/hongguo-crack-audit.yml 硬编码 v7.3.6.32 样本名+sha，scripts/hg1_*.py 三个脚本按旧包名写死输入
- 判定权在老马：你只产出机械事实+分类**建议**，不做终判

【EXPECTED OUTCOME】
1. 工作流参数化：新建/改造 audit workflow（建议输入版本号/资产名/sha 用 workflow_dispatch inputs 或复制为 hongguo-crack-audit-v737.yml，你判断哪种对既有 HG-1 线破坏最小），使 CI 可对 v7.3.7.32 双样本 dispatch；hg1_*.py 的输入路径参数化同理
2. 在本分支 hg3-cc dispatch CI 跑通一次全量审计（类集合 diff、画像重合率、端点提取+漂移、**com/b/a 家族在 v737 的存续点位复扫**=重点）
3. 产物 docs/hg3-diff-report-cc.md（含：run 号、每判据数字、classes24.dex 归属分析=真新增类还是重排、C 类候选清单+建议、与 HG-1 的增量对比）push 到 hg3-cc 分支
4. 中途被回收/超时前把「已到哪步+下一步」写进 docs/hg3-cc-context.md 留痕

【REQUIRED TOOLS】gh CLI（token 已在 agent 环境，git 网络走 http.proxy=http://192.168.1.150:30001）；GitHub Actions（ubuntu runner 有 JDK）；仓内现成 scripts/ 与 workflow 配方

【MUST DO】
- 每步改动小提交（Conventional Commits），push 到 hg3-cc 分支（远端分支不存在则 -u 创建）
- CI 失败时读 run 日志定位再修，重触发；注意 bash -e 下 grep -c 零计数炸 step（赋值尾缀 || true）
- 外层 zip 有 Overlapped entries，CI 里抽取用 unzip -o（python zipfile 读外层会 zip bomb 拒读；内层官方包正常）
- 报告数字必须来自 CI 实际产物（下载归档核验后引用），附 run 链接
- 完成断言三件套：分支 push 实锤 + run 号 conclusion=success + 报告路径，缺一不得声称完成

【MUST NOT DO】
- 禁止本机跑 apktool/baksmali/反编译/大 dex 处理（D7 铁律，重活全在 CI runner；本机只允许编辑脚本/gh 操作/读报告）
- 禁止 push main、禁止碰既有 v7.3.6.32 线产物（samples 旧资产、hg1/hg2 报告、旧 workflow 引用不动或向后兼容）
- 禁止上传大文件到任何地方（样本已在 Release）
- 不做净化 patch、不装机（后续轮次的活）
- 不修改 /root 下任何文件；只在你的工作目录内操作

【CONTEXT】双引擎对照实验：同一任务书另一个会话（Codex 引擎、hg3-codex 分支）并行独立执行，互不可见互不沟通，各自交付后老马对拍评分。老马已完成初检（结构/归属/内层官方包实锤），你无需重复 sha 级初检，直接进参数化+CI。任务线总文档在老马侧 /root/workspace/.tasks/hongguo-shortdrama/HG3-方案.md（你不可读，不必读）。有任何路线分叉按「对 HG-1 线破坏最小」原则自选并在报告写明理由。
